import { Book } from "./book";
import { User } from "./user";

export function filterBooks(books: Array<Book>, query: string): Array<Book> {
  return [];
}

export function filterUsers(users: Array<User>, query: string): Array<User> {
  return [];
}
